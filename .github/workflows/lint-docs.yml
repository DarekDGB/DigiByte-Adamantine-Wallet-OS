name: Lint Docs

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  docs-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Placeholder docs lint (non-blocking)
        run: |
          echo "Docs lint placeholder – not enforcing markdown rules yet."
          echo "This job always succeeds so CI stays green."
